function generate(
  targets,
  configurations,
  architectures) {
    return configurations.reduce((conf_acc, conf_cur) => {
      const current = architectures.reduce((arch_acc, arch_cur) => {

      }, []);
      return conf_acc;
    }, []);
}

module.exports = {
  generate
};